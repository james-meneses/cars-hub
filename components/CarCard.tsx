import { CarCardProps } from "@/types"

const CarCard = ({key, type, year, make, model, fuel_type} : CarCardProps) => {
  return (
    <li className="car-card" key={key}>
      
    </li>
  )
}

export default CarCard